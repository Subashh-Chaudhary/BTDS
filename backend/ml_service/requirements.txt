fastapi>=0.68.0,<0.69.0
uvicorn>=0.15.0,<0.16.0
python-multipart>=0.0.5,<0.1.0
# Use a newer Pillow release so pip can install a prebuilt wheel on Windows
# (removing the <9.0.0 upper bound which forced building older Pillow from source)
Pillow>=9.5.0
ultralytics>=8.0.0
python-dotenv>=0.19.0,<0.20.0
aiofiles>=0.7.0,<0.8.0